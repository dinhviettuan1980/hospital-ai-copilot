# .claude/ (Placeholder)

This directory will hold Claude Code project configuration specific to the
Hospital AI Copilot repository (e.g., project-level settings, custom
skills/agents, or permission rules useful for working on this codebase),
should the project choose to adopt them.

## Sprint 0 Status

**Empty by design.** No configuration is authored yet — this sprint is
documentation only.

## Rule

Any configuration added here must serve the development of this project's
in-scope modules (see
[../docs/03_SCOPE.md](../docs/03_SCOPE.md)) — it configures *how the
project is built*, not what the project *is*, and therefore never expands
or redefines project scope.
